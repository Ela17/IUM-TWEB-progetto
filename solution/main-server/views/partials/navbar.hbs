<nav class="navbar navbar-expand-lg navbar-dark fixed-top">
    <div class="container">
        <!-- Logo -->
        <a class="navbar-brand d-flex align-items-center" href="/">
            <i class="bi bi-camera-reels me-2 fs-4"></i>
            <span class="fw-bold">CinemaHub</span>
        </a>

        <!-- Mobile toggle button -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navigation Links -->
        <div class="collapse navbar-collapse" id="navbarNav">
            <!-- Left navigation -->
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link {{#if isHome}}active{{/if}}" href="/">
                        <i class="bi bi-house me-1"></i>Home
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if isMovies}}active{{/if}}" href="/movies">
                        <i class="bi bi-search me-1"></i>Movies Catalogue
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if isChat}}active{{/if}}" href="/chat">
                        <i class="bi bi-chat-dots me-1"></i>Live Chat
                    </a>
                </li>
            </ul>

            <!-- Right side: Search + Theme Toggle + Chat Status -->
            <div class="d-flex flex-column flex-lg-row align-items-center gap-3 mt-3 mt-lg-0">
                
                <!-- Quick Search -->
                <div id="quick-search" class="position-relative">
                    {{> search-bar}}
                    <!-- Search Results Container -->
                    <div id="search-results" class="search-results d-none">
                        <div class="search-results-content">
                            <!-- Popolato dinamicamente da main.js -->
                        </div>
                    </div>
                </div>

                <!-- Theme Toggle + Chat Status -->
                <div class="d-flex align-items-center gap-3">
                    <!-- Theme Toggle Button -->
                    <button 
                        id="theme-toggle" 
                        class="theme-toggle btn" 
                        type="button"
                        aria-label="Toggle theme"
                        title="Switch theme"
                    >
                        <span class="theme-toggle-icon">
                            <i class="bi bi-sun-fill theme-icon-sun"></i>
                            <i class="bi bi-moon-fill theme-icon-moon"></i>
                        </span>
                        <span class="d-none d-sm-inline ms-1" id="theme-text">Theme</span>
                    </button>

                    <!-- Chat Status Indicator -->
                    <div id="chat-indicator" class="text-light d-flex align-items-center">
                        <i class="bi bi-circle-fill text-success me-1"></i>
                        <small>
                            <span id="navbar-online-users">{{#if onlineUsers}}{{onlineUsers}}{{else}}0{{/if}}</span> online
                        </small>
                    </div>
                </div>
            </div>
        </div>
    </div>
</nav>